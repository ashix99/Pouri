Place your Yekan Bakh font files here if you want the PDF to use them by default:

- `YekanBakh-Regular.ttf`
- `YekanBakh-Bold.ttf`

If you use different filenames or another location, set `PDF_FONT_PATH` and `PDF_FONT_BOLD_PATH` in `.env`.
